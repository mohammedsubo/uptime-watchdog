# Monitoring Configuration
CHECK_INTERVAL=60       # Seconds between checks
HTTP_TIMEOUT=10         # Request timeout in seconds

# Database
WATCHDOG_DB=./data/watchdog.db

# Server (for local development)
HOST=0.0.0.0
PORT=8000
